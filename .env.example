SESSION_SECRET=changeme
PORT=3001

# Optional: set these to pre-configure the Connected App (or users enter them on the login form)
# SF_CLIENT_ID=
# SF_CLIENT_SECRET=

# Required in production — the public URL of your deployed app
# BASE_URL=https://your-app.onrender.com

# Optional: override the OAuth callback URL if different from BASE_URL/auth/callback
# SF_CALLBACK_URL=https://your-app.onrender.com/auth/callback

services:
  - type: web
    name: agentforce-readiness
    env: node
    buildCommand: npm install --production=false && npm run build
    startCommand: node server/index.js
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
